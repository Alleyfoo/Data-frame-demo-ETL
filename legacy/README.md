# Legacy notes

- Tkinter UI code remains in `src/app.py` and related files but is not exposed via the CLI.
- If you need to experiment with the Tkinter UI, run `python -m src.app` directly; it is not part of the primary demo flow.
